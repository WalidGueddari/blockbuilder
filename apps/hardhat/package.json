{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.19",
    "nodemon": "^3.1.9"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "scripts": {
    "watch:deploy": "nodemon --watch contracts --ext sol --exec 'npx hardhat compile && npx hardhat run scripts/deploy.ts --network localhost'"
  }
}
